# Base URL for the LTI backend API.
# Picked up at build time by Create React App (must start with REACT_APP_).
# Override per-environment (dev, staging, prod) by setting this var in the
# corresponding .env.development / .env.production file or in CI/hosting.
REACT_APP_API_BASE_URL=http://localhost:3010
